# Roamio backend (FastAPI + RAG planning agent) — for Render / Railway.
# Direct dependencies pinned to the versions developed against; pip resolves the rest.
fastapi==0.137.2
uvicorn[standard]==0.49.0
langchain-openai==1.3.2
langchain-chroma==1.1.0
langchain-core==1.4.7
langgraph==1.2.5
tavily-python==0.7.26
chromadb==1.5.9
pydantic==2.13.4
python-dotenv==1.2.2
supabase==2.31.0
requests==2.34.2          # used by corpus/ingest_content.py (offline grounding-content build)
